{
  "name": "chrome-heartass",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "site": "python3 -m http.server 8080",
    "agent": "cd chromie-lite && npm install && npm start"
  }
}
